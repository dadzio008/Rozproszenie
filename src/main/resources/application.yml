server:
  port: 80
  address: 0.0.0.0
spring:
  datasource:
    url: jdbc:mysql://mysql:3306/Rozproszenie?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false
    password: root
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  mvc:
    throw-exception-if-no-handler-found: true
  web:
    resources:
      add-mappings: false